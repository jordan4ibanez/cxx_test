# cxx_test
 
This is my first c++ project that actually runs.

You need clang++, make, meson, ninja, and mold. Disgusting, I know.

Then you run:

```bash
make reset

make
```

And if the c++ toolchain hasn't crashed at this point, you should have a terminal game.